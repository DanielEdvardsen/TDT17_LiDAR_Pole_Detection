max_lr: 0.01
weight_decay: 0.0001
momentum: 0.9

max_epochs: 50
image_size: 512
batch_size: 32
device: 0 # -1 for cpu, 0 for gpu
num_workers: 0
optimizer: sgd
initial_lr: 0.01
early_stopping_patience: 10

test_model: false
checkpoint_path: #checkpoints/*****.ckpt used for 

check_val_every_n_epoch: 3
devices: auto

wandb_project: LiDAR_Trondheim
wandb_run_name: test_2

train_split_ratio: 0.8


